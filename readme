Using this as a git sandbox.  Also notes some of my notes on how things are done and prevent dumb errors.

->To undo a commit do : git reset --hard HEAD~3 (where 3 is the number of commits to roll back)
-> git checkout otherbranch myfile.txt <- to copy a version of a single file from one branch to another (do this on the branch where the file should end up)
